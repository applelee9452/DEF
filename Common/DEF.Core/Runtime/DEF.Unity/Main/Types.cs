#if DEF_CLIENT

public enum RuntimeMode
{
    None = 0,
    HybridCLR
}

public enum TestMode
{
    None = 0,
    TestMode1,
    TestMode2,
    TestMode3,
}

public enum EditorRunSourcePlatform
{
    Android,
    iOS,
    PC,
}

#endif